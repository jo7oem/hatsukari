---
services:
  app:
    image: "golang:1.26"
    command: "./dev_tools/bin/air -c .air.toml"
    working_dir: /app
    volumes:
      - ./:/app
      - type: bind
        source: ./dev_tools/modcache
        target: /go/pkg/mod
    environment:
     SERVER_ADDRESS: ":8080"
     CONFIG_PATH: "hatsukari.yaml"
     WORK_DIR: "sample"
    ports:
        - "2345:2345"
        - "8080:8080"